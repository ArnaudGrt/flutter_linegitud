import 'package:flutter/material.dart';
import 'package:get/get.dart';

class NewLine extends StatelessWidget {
  const NewLine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("New Line")
    );
  }
}